import {StyleSheet} from 'react-native';

// Styles
const styles = StyleSheet.create({
  // screen: {flex: 1, justifyContent: 'center', alignItems: 'center',wid},
  screenText: {fontSize: 20, fontWeight: 'bold', color: '#333'},
});
export default styles;
