import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    overflow: 'scroll',
  },
  textWelcome: {
    padding: 5,
    fontSize: 20,
  },
  textCurrentLocation: {
    padding: 5,
    fontSize: 15,
  },
});
export default styles;
