import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginLeft: 20,
  },
  buttonWrapper: {gap: 20, flexDirection: 'row'},
  activityBar: {marginTop: 20},
});

export default styles;
