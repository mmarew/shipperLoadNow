import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  processWrapper: {flex: 1},
  mapContainer: {flex: 1, height: '80%'},
  infoContainer: {backgroundColor: 'white', height: '20%', padding: 20},
});

export default styles;
