import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {padding: 10, margin: 10, flex: 1, marginTop: 100},
  shipingCost: {paddingLeft: 70},
});
export default styles;
